@model UserProfileViewModel

@{
    ViewBag.Title = "Meu perfil";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">@Model.NickName</h3>
    </div>
    <div class="card-body">
    </div>
</div>